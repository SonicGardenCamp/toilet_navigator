<% flash.each do |key, value| %>
  <div class="alert alert-<%= key == 'notice' ? 'suceess' : 'danger' %>">
    <span><%= value %></span>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>